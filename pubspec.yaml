name: webrtc_plugin
description: Flutter WebRTC plugin.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^0.6.0
  mockito: ^4.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    androidPackage: com.shepeliev.webrtc_plugin
    pluginClass: WebrtcPlugin
